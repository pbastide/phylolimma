# Get all paramters but lower and upper bounds and starting value

Get all parameters but `lower.bounds`, `upper.bound` and
`starting.value`

## Usage

``` r
get_dots_args(...)
```

## Arguments

- ...:

  user specified parameters

## Value

list of parameters
