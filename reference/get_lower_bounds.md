# Get lower bounds on parameters

Get the lower bounds on the parameters

## Usage

``` r
get_lower_bounds(bounds_alpha, min_sigma2_error, ...)
```

## Arguments

- ...:

  user specified parameters

## Value

list of lower bounds on parameters
