# Get upper bounds on parameters

Get the upper bounds on the parameters

## Usage

``` r
get_upper_bounds(bounds_alpha, min_sigma2_error, ...)
```

## Arguments

- ...:

  user specified parameters

## Value

list of upper bounds on parameters
