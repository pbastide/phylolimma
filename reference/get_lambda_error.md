# Get lambda value

Compute the lambda value equivalent to the sigma2 plus sigma2_error
model.

## Usage

``` r
get_lambda_error(sigma2, sigma2_error, h_tree)
```

## Arguments

- sigma2:

  phylogenetic variance

- sigma2_error:

  measurement error variance

- h_tree:

  height of the tree

## Value

lambda_error
